## To Install & Run

1. Go into the `backend` folder and run:
    ```bash
    npm install
    npm start
    ```

2. Go into the `frontend` folder and run:
    ```bash
    npm install
    npm start
    ```